  steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/turnkey-world-334318/guehelloworld2:$COMMIT_SHA', '.']
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/turnkey-world-334318/guehelloworld2:$COMMIT_SHA']
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
    - 'run'
    - 'deploy'
    - 'guehelloworld2'
    - '--image'
    - 'gcr.io/turnkey-world-334318/guehelloworld2:$COMMIT_SHA'
    - '--region'
    - 'europe-west1'
  images:
  - 'gcr.io/turnkey-world-334318/guehelloworld2:$COMMIT_SHA'

